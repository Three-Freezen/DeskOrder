<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- ═══════════════════════════════════════════
       ScrollBar — spec §5.5
       10px wide, no arrow buttons, transparent track.
       Thumb: 4px wide (vertical) / 4px tall (horizontal), 2px corner radius,
              text.tertiary @ 40% opacity.
       Hover: 6px wide, @ 70%, fast transition.
       ═══════════════════════════════════════════ -->

  <Style TargetType="ScrollBar">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Tertiary}"/>
    <Setter Property="MinWidth" Value="10"/>
    <Setter Property="MinHeight" Value="10"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ScrollBar">
          <Grid x:Name="GridRoot"
                Width="{TemplateBinding Width}"
                Height="{TemplateBinding Height}"
                MinWidth="10" MinHeight="10"
                Background="{TemplateBinding Background}">

            <Track x:Name="PART_Track"
                   Orientation="{TemplateBinding Orientation}"
                   IsDirectionReversed="True">
              <Track.DecreaseRepeatButton>
                <RepeatButton Focusable="False"
                              OverridesDefaultStyle="True">
                  <RepeatButton.Template>
                    <ControlTemplate TargetType="RepeatButton">
                      <Border Background="Transparent"/>
                    </ControlTemplate>
                  </RepeatButton.Template>
                </RepeatButton>
              </Track.DecreaseRepeatButton>
              <Track.IncreaseRepeatButton>
                <RepeatButton Focusable="False"
                              OverridesDefaultStyle="True">
                  <RepeatButton.Template>
                    <ControlTemplate TargetType="RepeatButton">
                      <Border Background="Transparent"/>
                    </ControlTemplate>
                  </RepeatButton.Template>
                </RepeatButton>
              </Track.IncreaseRepeatButton>
              <Track.Thumb>
                <Thumb x:Name="Thumb"
                       MinHeight="20" MinWidth="20"
                       Focusable="False"
                       OverridesDefaultStyle="True"
                       Background="#66767676"/>
              </Track.Thumb>
            </Track>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Thumb inside the ScrollBar: pick up orientation from ScrollBar ancestor. -->
  <Style TargetType="Thumb">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Thumb">
          <Border x:Name="ThumbBd"
                  CornerRadius="2"
                  Background="{TemplateBinding Background}"/>
          <ControlTemplate.Triggers>
            <!-- Vertical bar: thumb 4px wide centered, hover → 6px @ 70% -->
            <DataTrigger Value="Vertical">
              <DataTrigger.Binding>
                <Binding Path="Orientation"
                         RelativeSource="{RelativeSource AncestorType=ScrollBar}"/>
              </DataTrigger.Binding>
              <Setter TargetName="ThumbBd" Property="Width" Value="4"/>
              <Setter TargetName="ThumbBd" Property="HorizontalAlignment" Value="Center"/>
            </DataTrigger>
            <DataTrigger Value="Horizontal">
              <DataTrigger.Binding>
                <Binding Path="Orientation"
                         RelativeSource="{RelativeSource AncestorType=ScrollBar}"/>
              </DataTrigger.Binding>
              <Setter TargetName="ThumbBd" Property="Height" Value="4"/>
              <Setter TargetName="ThumbBd" Property="VerticalAlignment" Value="Center"/>
            </DataTrigger>
            <!-- Hover over the bar widens the thumb via ancestor trigger forwarding. -->
            <DataTrigger Value="True">
              <DataTrigger.Binding>
                <Binding Path="IsMouseOver"
                         RelativeSource="{RelativeSource AncestorType=ScrollBar}"/>
              </DataTrigger.Binding>
              <Setter TargetName="ThumbBd" Property="Background" Value="#B3767676"/>
            </DataTrigger>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Value="Vertical">
                  <Condition.Binding>
                    <Binding Path="Orientation"
                             RelativeSource="{RelativeSource AncestorType=ScrollBar}"/>
                  </Condition.Binding>
                </Condition>
                <Condition Value="True">
                  <Condition.Binding>
                    <Binding Path="IsMouseOver"
                             RelativeSource="{RelativeSource AncestorType=ScrollBar}"/>
                  </Condition.Binding>
                </Condition>
              </MultiDataTrigger.Conditions>
              <Setter TargetName="ThumbBd" Property="Width" Value="6"/>
            </MultiDataTrigger>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Value="Horizontal">
                  <Condition.Binding>
                    <Binding Path="Orientation"
                             RelativeSource="{RelativeSource AncestorType=ScrollBar}"/>
                  </Condition.Binding>
                </Condition>
                <Condition Value="True">
                  <Condition.Binding>
                    <Binding Path="IsMouseOver"
                             RelativeSource="{RelativeSource AncestorType=ScrollBar}"/>
                  </Condition.Binding>
                </Condition>
              </MultiDataTrigger.Conditions>
              <Setter TargetName="ThumbBd" Property="Height" Value="6"/>
            </MultiDataTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>